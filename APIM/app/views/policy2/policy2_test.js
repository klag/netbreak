'use strict';

describe('APIM.policy2 module', function() {

  beforeEach(module('APIM.policy2'));

  describe('policy2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var policy2_ctrl = $controller('policy2_ctrl');
      expect(policy2_ctrl).toBeDefined();
    }));

  });
});