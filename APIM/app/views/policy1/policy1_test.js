'use strict';

describe('APIM.policy1 module', function() {

  beforeEach(module('APIM.policy1'));

  describe('policy1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var policy1_ctrl = $controller('policy1_ctrl');
      expect(policy1_ctrl).toBeDefined();
    }));

  });
});