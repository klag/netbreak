'use strict';

describe('APIM.login module', function() {

  beforeEach(module('APIM.login'));

  describe('login controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var login_ctrl = $controller('login_ctrl');
      expect(login_ctrl).toBeDefined();
    }));

  });
});